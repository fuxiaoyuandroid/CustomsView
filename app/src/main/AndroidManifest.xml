<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cv.customviews">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".countstep.StepActivity" />
        <activity android:name=".fontcolorchange.ViewPagerActivity" />
        <activity android:name=".loading.LoadingActivity" />
        <activity android:name=".rating.RatingActivity" />
        <activity android:name=".lettersearch.LetterSearchActivity" />
        <activity android:name=".viewsource.ViewDemoActivity" />
        <activity android:name=".stream.StreamActivity" />
        <activity android:name=".viewsource.TouchActivity" />
        <activity android:name=".KgSlidingMenu.SlidingMenuActivity"/>

        <activity android:name=".QqSlidingMenu.QqSlidingMenuActivity"/>

        <activity android:name=".carhome.VerticalDragActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>